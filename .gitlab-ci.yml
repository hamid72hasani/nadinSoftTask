shell-smoke:
  stage: test
  tags: "shell"
  variables:
    CI_DEBUG_TRACE: "true"
  script:
    - set -xeuo pipefail
    - echo "whoami=$(whoami)"
    - echo "pwd=$(pwd)"
    - echo "CI_PROJECT_DIR=${CI_PROJECT_DIR:-unset}"
    - ls -la || true
    - echo OK
