groups:
  - name: basic
    rules:
      - alert: NoTraffic
        expr: rate(app_requests_total[1m]) == 0
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "No incoming traffic"
          description: "No requests seen in the last minute."
